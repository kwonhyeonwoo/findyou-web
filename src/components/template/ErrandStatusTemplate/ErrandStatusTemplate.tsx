import { useParams } from "next/navigation";
import React from "react";

const ErrandStatusTemplate = () => {
  const { id } = useParams();
  return <div>ErrandStatusTemplate</div>;
};

export default ErrandStatusTemplate;
